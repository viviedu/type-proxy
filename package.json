{
  "name": "vivi-type-proxy",
  "version": "1.0.0",
  "description": "Validate data with unknown structure",
  "author": "Andrew Pritchard <andrew@vivi.io>",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "yarn run eslint . && yarn run tsc",
    "test": "yarn run build && yarn run mocha",
    "prepare": "tsc"
  },
  "files": [
    "dist/**/*"
  ],
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.4.0",
    "@typescript-eslint/parser": "^5.4.0",
    "eslint": "^8.3.0",
    "mocha": "^9.1.3",
    "typescript": "^4.5.2"
  }
}
